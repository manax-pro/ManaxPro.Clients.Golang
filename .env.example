# Example environment variables for manax-go
#
# Copy this file to ".env" and adjust values for your environment.
# This file MUST NOT be committed with real secrets.

# Base URL of the Manax ApiService.
# Example: https://api.manax.pro or http://localhost:5010
MANAX_BASE_URL=http://localhost:5010

# Optional API key for privileged operations (e.g. creating wallets).
MANAX_KEY=changeme-dev-only

# Optional storage-related key, if ApiService expects it in headers.
MANAX_STORAGE_KEY=changeme-storage-key

# Logical profile identifier and token, used as X-Pro-Id / X-Pro-Token.
MANAX_PRO_ID=
MANAX_PRO_TOKEN=

# Timeout (in seconds) for HTTP requests.
MANAX_HTTP_TIMEOUT_SEC=30

# SSE streaming: per-connection idle timeout (seconds).
MANAX_SSE_IDLE_TIMEOUT_SEC=300

# If set to "1", log each HTTP request/response (body is NOT logged by default).
MANAX_HTTP_DEBUG=0

# Optional custom User-Agent to send with requests.
MANAX_USER_AGENT=manax-go/0.1.0

# Maximum number of facts to fetch in snapshot.
MANAX_FACTS_SNAPSHOT_LIMIT=500

# Maximum number of matches to fetch in snapshot.
MANAX_MATCHES_SNAPSHOT_LIMIT=500

# Minimum score for matches (0.0-1.0). Leave empty to disable filtering.
MANAX_MATCHES_MIN_SCORE=

# Direction for matches snapshot: Offer or Seek.
MANAX_MATCHES_DIRECTION=Offer